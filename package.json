{
  "name": "invigilator",
  "version": "0.1.0",
  "private": true,
  "description": "Checks generated reports against the data they claim to describe. App-agnostic: one engine, one config file per app.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
